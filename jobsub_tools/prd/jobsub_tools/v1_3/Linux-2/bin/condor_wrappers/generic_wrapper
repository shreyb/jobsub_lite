#!/bin/sh

# A wrapper for remote Condor access
# 2008-11-04 Ryan Patterson

BIN=`basename $0`
if [ "`hostname`" = "if05.fnal.gov" ]
then
  /opt/condor/bin/${BIN} $@
else
  ssh -ax if05.fnal.gov "cd `pwd` ; /opt/condor/bin/${BIN} $@"
fi

